"""Performance module — load testing with Locust + JMeter (no Playwright).

Heavy tools (locust) are imported lazily inside locust/ only, so importing this
package never requires the ``perf`` extra.
"""
