# Issue Note Example

Used as the **note-context fallback** input when the Jira MCP is unavailable
(STEP 0/1). Paste this kind of content (or point the agent at `docs/ai/`).

Title: Login with password

Acceptance Criteria:
- AC1: User can log in with valid email and password
- AC2: User sees validation when password is missing
- AC3: User sees error when credentials are invalid

Figma:
https://figma.example.com/file/abc123

Prototype:
https://prototype.example.com/login

Notes:
- Login page should be responsive
- Add data-test-id for login form elements if missing
