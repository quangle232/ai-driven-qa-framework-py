# GitLab adapter for the ship step (create_gitlab_mr.py).
# Copy to environments/.env.gitlab (gitignored) and fill in — or set env vars.
GITLAB_URL=https://gitlab.example.com
GITLAB_TOKEN=glpat-your-token-here
GITLAB_PROJECT_ID=123
